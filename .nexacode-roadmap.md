# NexaCode Code Agent 路线图

> 当前 NexaCode 是一个多模型 AI 聊天桌面客户端，要进化为真正的 **Code Agent**（如 Cursor、Claude Code、Windsurf、Aider 等），需要以下核心功能和架构设计。

---

## 一、当前状态 vs 目标状态

| 维度 | 当前状态 | 目标状态 (Code Agent) |
|------|----------|----------------------|
| 交互模式 | 纯聊天对话 | 聊天 + 工具调用 + 代码编辑闭环 |
| 代码感知 | 无 | 理解项目结构、读取/编辑代码文件 |
| 工具能力 | 无 | 文件读写、终端执行、搜索、Git 操作等 |
| 上下文管理 | 全量消息历史 | 智能上下文窗口（检索 + 压缩 + 裁剪） |
| Agent 循环 | 单轮 LLM 调用 | 多步推理 + 自主决策循环 |
| 编辑体验 | 无 | 行级 Diff 预览 + 确认/拒绝 + 自动应用 |
| 安全性 | 无 | 沙箱执行 + 用户确认机制 + 权限控制 |

---

## 二、核心功能模块

### 1. 🔧 Tool System（工具系统）— 最重要的基础

Code Agent 的核心区别在于 **LLM 能够调用工具来操作代码和环境**。

#### 工具清单

对比业界主流 Code Agent 的工具设计：

| 工具 | Claude Code | Cline | Aider | NexaCode | 状态 |
|------|:-----------:|:-----:|:-----:|:--------:|:----:|
| 读取文件 | ✅ Read | ✅ read_file | ✅ Read | ✅ Read | ✅ 已有 |
| 写入/创建文件 | ✅ Write | ✅ write_to_file | ✅ Write | ✅ Write | ✅ 已有 |
| 列出目录 | ✅ LS | ✅ list_files | - | ✅ LS | ✅ 已有 |
| 内容搜索 | ✅ Grep | ✅ search_files | - | ✅ Grep | ✅ 已有 |
| 文件名搜索 | ✅ Glob | - | - | ❌ 缺失 | 🔴 需新增 |
| 编辑文件 | ✅ Edit | ✅ apply_diff | ✅ SEARCH/REPLACE | ✅ Edit | ✅ 已有 |
| 多处同时编辑 | ✅ MultiEdit | - | - | ❌ 缺失 | 🔴 需新增 |
| 终端命令 | ✅ Bash | ✅ execute_command | ✅ Run | ✅ Bash | ✅ 已有 |
| 网页获取 | ✅ WebFetch | - | - | ✅ WebFetch | ✅ 已有 |
| 网页搜索 | ✅ WebSearch | - | - | ❌ 缺失 | 🟡 需新增 |
| 任务管理 | ✅ Task | ✅ (Plan/Act) | - | ❌ 缺失 | 🔴 需新增 |
| 诊断/错误读取 | - | ✅ list_errors | - | ✅ Diagnostic | ✅ 已有 |
| Git 操作 | - (用 Bash) | - (用 Bash) | ✅ (内置) | ✅ Git | ✅ 已有 |
| 浏览器操作 | - | ✅ browser_action | - | ❌ 缺失 | 🟡 需新增 |
| 图片读取 | ✅ (Read 支持) | ✅ (截图) | - | ❌ 缺失 | 🟡 需新增（扩展 Read） |
| MCP 扩展 | ✅ | ✅ | - | ❌ 缺失 | 🟢 远期 |

🔴 = 必须新增 | 🟡 = 建议新增 | 🟢 = 远期目标

#### 工具目录结构

```
crates/nexacode-core/src/tools/
├── mod.rs              # Tool trait 定义 & 工具注册表
│
│  ─── 文件操作 ───
├── read.rs             # Read  — 读取文件内容（支持文本/图片/行范围）
├── write.rs            # Write — 写入/创建文件
├── ls.rs               # LS    — 列出目录内容（递归/非递归）
├── grep.rs             # Grep  — 文件内容搜索（ripgrep 集成，正则 + 行号）
├── glob.rs             # 🔴 Glob — 文件名模式搜索（如 **/*.rs, src/**/*.ts）
│
│  ─── 代码编辑 ───
├── edit.rs             # Edit       — 精确代码编辑（SEARCH/REPLACE 模式）
├── multi_edit.rs       # 🔴 MultiEdit — 多文件/多处同时编辑（原子性）
│
│  ─── 执行环境 ───
├── bash.rs             # Bash — 终端命令执行（带超时、沙箱、输出截断）
├── git.rs              # Git  — Git 操作（diff, log, commit, checkout, stash）
│
│  ─── 信息获取 ───
├── web_fetch.rs        # WebFetch   — 获取网页内容
├── web_search.rs       # 🟡 WebSearch  — 网页搜索
├── diagnostic.rs       # Diagnostic — 读取 Linter/编译器错误
│
│  ─── 任务管理 ───
├── task.rs             # 🔴 Task — 子任务管理（拆解、进度追踪）
│
│  ─── 浏览器 ───
└── browser.rs          # 🟡 Browser — 无头浏览器操作
```

#### 命名原则

- 参考 Claude Code 的简洁大写风格（Read/Write/Edit/Bash/Grep/Glob）
- 文件名用 snake_case（Rust 惯例），工具名用 PascalCase（LLM 调用名）
- 一个工具一个文件，文件名与工具名对应（read.rs → Read, bash.rs → Bash）
- 去掉冗余前缀（fs_read → Read, code_edit → Edit, terminal → Bash）

#### Tool Trait 关键设计点

- `name()` / `description()` / `parameters()` — 基础三件套，告诉 LLM 工具是什么、怎么用
- `risk_level()` — 🔴 风险等级（Safe / Moderate / Dangerous），决定是否需要用户确认
- `requires_confirmation()` — 🔴 根据参数动态判断是否需要确认（如 rm -rf vs ls）
- `validate()` — 🔴 执行前校验（如：Edit 前必须先 Read，防止盲目编辑）
- `render_call()` / `render_result()` — 🔴 前端渲染（Diff 预览、终端输出等不同展示方式）

#### Tool Registry 关键设计点

- 工具注册、查找、生成给 LLM 的 ToolDefinition
- 🔴 工具分组（文件操作 / 代码编辑 / 执行环境），用于前端展示和权限控制
- 🔴 按风险等级筛选工具
- 🔴 动态启用/禁用工具

#### ToolContext 关键设计点

- working_dir / permissions / timeout — 基础上下文
- 🔴 session_id — 当前会话 ID
- 🔴 file_cache — 文件读取缓存（Claude Code 核心设计：Edit 前必须先 Read）

#### FileCache 关键设计点

- LLM 必须先 Read 文件，才能 Edit 文件
- 防止 LLM 盲目编辑（不知道文件当前内容）
- 文件修改后自动更新缓存

#### ToolResult 关键设计点

- output / is_error / metadata — 基础三件套
- 🔴 truncated — 输出过长时截断，但必须告知 LLM "结果被截断"
- 🔴 total_bytes — 原始输出长度

#### 🔴 必须新增的工具

**1. Glob（文件名模式搜索）**

- Agent 理解项目的第一步是"找文件"，光有 Grep（内容搜索）不够
- LS 列出整个目录树太慢、结果太多，Glob 支持模式匹配精准定位
- 参数：pattern（glob 模式）、path（搜索根目录）、exclude（排除模式）
- Claude Code 的 Glob 使用频率极高

**2. MultiEdit（多处同时编辑）**

- 重命名变量、修改 API 等场景需要同时修改多个文件的多处代码
- 原子操作：所有编辑要么全部成功，要么全部回滚
- 执行逻辑：校验所有 old_string 存在且唯一 → 按文件分组 → 文件内从后往前替换（避免行号偏移）

**3. Task（任务管理）**

- 复杂任务需要拆解和追踪进度
- 支持操作：create（创建任务列表）、update（更新状态）、delegate（委派子 Agent）、list（查看进度）
- 任务之间可设置依赖关系
- 参考 Claude Code 的 Task 工具

#### 🟡 建议新增的工具

**4. Read 图片模式**

- 扩展现有 Read 工具，根据文件扩展名自动判断类型
- 文本文件返回内容，图片文件通过多模态 API（vision）理解
- 无需独立工具，Claude Code 也是这么做的

**5. WebSearch（网页搜索）**

- Agent 遇到不熟悉的库或 API 时，需要搜索文档和解决方案
- 返回搜索结果摘要（标题、URL、片段）

**6. Browser（无头浏览器操作）**

- Cline 的特色工具，Agent 可以启动网站、点击按钮、截图验证 UI
- 支持操作：launch、click、type、screenshot、console、close

#### 🟢 远期目标

**7. MCP 扩展接口**

- Model Context Protocol，连接外部工具和数据源（数据库、API、自定义工具）

---

### 2. 🔄 Agent Loop（Agent 循环）— 核心执行引擎

当前是"用户发消息 → LLM 回复"，需要变为"用户发消息 → LLM 推理 → 调用工具 → 观察结果 → 继续推理 → ... → 最终回复"。

#### 核心流程

1. 用户发送消息
2. 调用 LLM（带工具定义）
3. 检查 LLM 响应：
   - 纯文本回复 → 循环结束，返回给用户
   - 工具调用 → 执行工具，将结果反馈给 LLM → 回到步骤 2
4. 重复直到 LLM 给出最终文本回复或达到最大迭代次数

#### 关键设计要点

- **最大迭代限制**：防止 Agent 陷入死循环（建议默认 50 次）
- **流式输出**：Agent Loop 的每一步都需要实时推送到前端，让用户看到 Agent 在"思考"和"行动"
- **中断机制**：用户随时可以中断 Agent 的执行
- **回滚支持**：Agent 执行的文件修改需要可回滚
- **自审步骤**：编辑后自动插入验证提示（运行 build、重新读取修改后的文件）

---

### 3. 🧠 Context Management（上下文管理）— 决定 Agent 智能水平

当前直接将全部消息发给 LLM，很快会超出上下文窗口。需要智能上下文管理。

#### 需要解决的问题

1. **Token 预算**：不同模型有不同的上下文窗口大小
2. **相关性排序**：哪些文件/代码片段与当前任务最相关
3. **上下文压缩**：历史对话的摘要与裁剪
4. **项目级上下文**：项目结构、依赖关系、代码索引

#### 上下文来源

| 来源 | 说明 | 优先级 |
|------|------|--------|
| SystemPrompt | 系统提示词 | 最高 |
| ConversationHistory | 对话历史（带压缩） | 高 |
| ProjectStructure | 项目目录树 | 高 |
| OpenFiles | 用户当前打开的文件 | 高 |
| CodeSearchResults | 代码搜索结果 | 中 |
| LinterErrors | 编译/Lint 错误 | 中 |
| GitDiff | 未提交的更改 | 中 |
| FileContent | 特定文件内容 | 按需 |
| DocumentationIndex | 项目文档索引 | 低 |

#### Token 预算分配策略

| 类别 | 预算占比 | 说明 |
|------|----------|------|
| 系统提示词 | ~3% | 项目信息、工具定义、规则 |
| 项目结构 | ~1.5% | 精简目录树 |
| 当前打开文件 | ~8% | 与任务最相关 |
| 搜索/检索结果 | ~15% | Grep/Glob 结果 |
| 对话历史 | ~23% | 压缩后的历史 |
| 预留给 LLM 输出 | ~48% | 留足生成空间 |

#### 项目代码索引

- 启动时索引项目，支持增量更新
- 功能：语义搜索、项目结构摘要、文件内容读取（带行号）
- 远期：符号索引（函数、类、变量）、向量索引（Embedding 语义搜索）

---

#### 🔴 压缩（Compression）— 最核心的机制

压缩的目标是：在 Token 不够时，用更少的 Token 保留核心信息。参考 Claude Code 的三层渐进式压缩架构，从低代价到高代价逐级升级。

**第一层：MicroCompact（轻量清理）**

| 维度 | 说明 |
|------|------|
| 核心思想 | 不改变对话结构，只清除过期的工具返回内容 |
| 触发时机 | 每次 API 调用前自动执行（对用户不可见） |
| 操作 | 将 >3 轮前的 tool_result 替换为简短摘要 |
| 示例 | Read 的完整文件内容 → `[Previous: Read src/main.rs (245 lines)]` |
| KV Cache | 不破坏，通过注意力掩码实现"软删除" |
| 代价 | 最低，"日常保洁"级别 |

为什么先清工具结果？工具输出是上下文最大的吞噬者：Read 一个 1000 行文件吃掉 ~4000 tokens，Bash 输出可能几千行。这些结果在 3 轮后对当前任务已经不重要，但还在白白占用 Token。

**第二层：AutoCompact（自动摘要压缩）**

| 维度 | 说明 |
|------|------|
| 核心思想 | Token 使用量达到阈值时，用 LLM 生成对话摘要替换完整历史 |
| 触发时机 | Token 使用量达到 effective_window - 13K（约 87%） |
| KV Cache | 会破坏，需要重新计算 |
| 熔断器 | 连续 3 次压缩失败则停止自动压缩 |

AutoCompact 核心流程：
1. 分组 — 将对话历史分组（连续的 assistant + tool_result 为一组）
2. 剥离图片 — 图片占 Token 极多，摘要中用描述替代
3. 生成摘要 — 调用 LLM 生成摘要，保留关键决策、文件编辑记录、当前任务状态、未完成事项；丢弃冗余探索过程、重复搜索结果、中间调试步骤
4. 恢复元数据 — 从 tool_result 中提取已读取/编辑的文件列表、已使用的技能/命令、项目当前状态描述
5. 替换 — 用摘要替换完整的对话历史

**第三层：Full Compact / 手动 Compact**

| 维度 | 说明 |
|------|------|
| 核心思想 | 兜底策略，用户主动触发或系统强制执行 |
| 触发时机 | 用户输入 `/compact` 命令、AutoCompact 失败后兜底、上下文即将溢出 |
| 与 AutoCompact 区别 | 支持自定义指令（用户可指定"重点关注认证模块的修改"）、更激进的压缩策略、可能丢弃更多细节 |

**第四层：Context Collapse（上下文折叠）**

| 维度 | 说明 |
|------|------|
| 核心思想 | 消息数量超过阈值时，折叠整段对话 |
| 触发时机 | 消息数量超过阈值（而非 Token 阈值） |
| 操作 | 将连续的多轮对话折叠为单条摘要消息 |
| 代价 | 破坏 KV Cache，比 AutoCompact 更激进 |

**Token 阈值体系（参考 Claude Code 设计）**

| 阈值 | 计算方式 | 说明 |
|------|----------|------|
| AutoCompact 触发 | effective_window - 13K tokens | ~87% 上下文使用率 |
| 警告提示 | threshold - 20K tokens | ~90% 时提示用户 |
| 错误状态 | threshold - 20K tokens | 高使用率时的错误状态 |
| 阻塞新请求 | effective_window - 3K tokens | ~97% 时阻止新请求 |

---

#### 🔴 检索（Retrieval）— 如何找到相关上下文

检索的目标是：从项目代码库中找到与当前任务最相关的代码片段，填入有限的上下文窗口中。

**方式 1：基于工具的即时检索（Phase 3 实现，最可靠）**

Agent 通过工具主动搜索，不需要预处理，实时准确。

工作流程示例：
- 用户："修复登录页面的 Bug"
- Agent：Glob(`**/login*`) → 找到所有登录相关文件
- Agent：Grep(`login|auth|sign_in`, path=`src/`) → 搜索相关代码
- Agent：Read(`src/pages/Login.tsx`) → 读取最相关的文件
- Agent：Grep(`handleLogin|onSubmit`, path=`src/pages/Login.tsx`) → 精确定位

优点：不需要预处理，实时准确
缺点：依赖 Agent 的搜索策略，多步搜索消耗 Token

**方式 2：基于 Embedding 的语义检索（RAG，Phase 5 实现）**

构建索引阶段（项目打开时）：
1. 解析代码 → 用 AST (Tree-sitter) 智能分块（按函数/类边界切分，保持语义完整，非按行数切割）
2. Embedding 向量化 → 将每个代码块转为高维向量（本地模型 ort + small model 或远程 API）
3. 存入向量数据库 → SQLite-VSS / Qdrant / Milvus
4. 增量更新 → 文件修改后只更新变更的部分

查询阶段（用户提问时）：
1. 将用户问题 Embedding 为向量
2. 在向量数据库中做混合搜索（稠密检索：向量相似度 → 语义相关 + 稀疏检索：BM25 关键词 → 精确匹配）
3. Reranker 重排序 → 用 Cross-Encoder 对候选结果精排
4. 按 Token 预算截取 Top-K 结果
5. 注入 LLM 上下文

Embedding 索引关键设计点：

| 维度 | 设计选择 | 说明 |
|------|----------|------|
| 分块策略 | AST 感知分块 | 按 Tree-sitter 解析的函数/类边界切分，保持语义完整 |
| 分块大小 | 512-1024 tokens | 太小丢失上下文，太大检索不精确 |
| 混合检索 | 稠密 + 稀疏 | 语义搜索 + 关键词匹配互补 |
| Token 预算 | 检索结果最多占 ~15% | 不能让搜索结果挤占对话空间 |
| 缓存 | Embedding 结果缓存 | 避免重复计算，文件未变则不重新 Embedding |

**方式 3：基于符号索引的精确检索（LSP，Phase 5 实现）**

通过 Language Server Protocol 实现精确的代码导航：
- goto_definition → 跳转到函数/类型定义
- find_references → 查找所有引用位置
- get_hover_info → 获取类型信息和文档
- get_diagnostics → 获取编译/Lint 错误

工作流程示例：
- 用户："重命名 UserService 为 AccountService"
- Agent：find_references(`UserService`) → 找到 23 处引用
- Agent：逐文件执行 Edit → 23 处全部替换
- Agent：get_diagnostics() → 确认无编译错误

---

#### 🔴 裁剪（Trimming）— 如何砍掉不必要的内容

裁剪的目标是：主动减少单条消息或单个工具结果的大小，而不是删除整条消息。

**策略 1：工具输出截断**

| 工具 | 截断策略 | 截断提示 |
|------|----------|----------|
| Read | 超过 500 行的文件只返回前 200 行 + 后 100 行，中间用截断标记替代 | `...(N lines truncated, use Read with line range to see more)` |
| Bash | stdout/stderr 各保留最近 200 行，前面用截断标记替代 | `...(N lines truncated)` |
| Grep | 每个文件最多显示 5 个匹配，总结果最多显示 50 个文件 | `Showing top 50 of N files. Use more specific pattern to narrow results.` |
| LS | 每层最多 20 个条目，深度限制 3 层 | `...(N more items, use LS with specific path to explore)` |

关键设计：截断后必须告知 LLM "结果被截断"，让 Agent 可以主动请求更多内容（如 Read 指定行范围）。

**策略 2：上下文优先级排序与裁剪**

当 Token 预算不够时，按优先级从低到高裁剪：

| 优先级 | 上下文内容 | 裁剪策略 |
|--------|-----------|----------|
| 1（最高） | System Prompt | 必须保留 |
| 2 | 当前任务的上下文 | 必须保留 |
| 3 | 最近 3 轮完整对话 | 高保留 |
| 4 | 正在编辑的文件内容 | 高保留 |
| 5 | 搜索结果 | 中保留（可减少条数） |
| 6 | 更早的对话历史 | 低保留（可压缩） |
| 7 | 项目目录树 | 低保留（可精简） |
| 8 | 文档索引 | 最低保留（可丢弃） |

**策略 3：对话历史滑动窗口**

| 轮次范围 | 保留策略 | 说明 |
|----------|----------|------|
| 最近 3-5 轮 | 完整保留 | 最相关的上下文 |
| 中间轮次 | 压缩为摘要 | 保留关键决策和操作 |
| 过早轮次 | 丢弃 | 与当前任务无关 |

滑动窗口预算示例（200K 窗口）：
- System Prompt: 3K
- 项目结构: 1.5K
- 对话摘要（轮 1-15）: 4K（压缩后）
- 完整对话（轮 16-18）: 12K（最近 3 轮）
- 当前文件内容: 8K
- 搜索结果: 15K
- 预留给 LLM 输出: 96K
- 总计: ~139.5K / 200K

**策略 4：智能内容精简**

| 内容类型 | 精简方式 | 说明 |
|----------|----------|------|
| 代码文件 | 删除空行和注释（可选）、压缩 import 语句、只返回函数签名 + 关键实现 | Agent 可控制精简程度 |
| 目录树 | 深度限制（最多 3 层）、隐藏 node_modules/.git/target 等、每层最多 20 个条目 | 减少无关信息 |
| 系统提示词 | 项目指令尽量精简、工具描述按需加载 | 当前任务不需要的工具不注入 |

---

#### 三者协同：上下文管理实时管道

上下文的检索、压缩、裁剪不是独立的，而是一个实时运行的管道：

```
用户消息进入
    │
    ▼
┌─────────────────────────────┐
│  1. Token 预算计算          │
│     剩余 = model_limit      │
│            - system_prompt   │
│            - reserved_output │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  2. 检索（Retrieval）       │
│     - 自动注入打开文件      │
│     - 项目结构摘要          │
│     - 按需 Embedding 搜索   │
│     - 控制检索量 ≤ 15% 预算 │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  3. 裁剪（Trimming）        │
│     - 工具输出截断           │
│     - 代码文件精简           │
│     - 目录树深度限制         │
│     - 按优先级排序和裁剪     │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  4. 压缩（Compression）     │
│     - MicroCompact: 清旧结果 │
│     - AutoCompact: 摘要历史  │
│     - Full Compact: 兜底压缩 │
│     - 检查是否仍超预算       │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  5. 组装最终上下文           │
│     - 检查 Token 总量       │
│     - 如果仍超 → 回到步骤 3  │
│     - 如果 OK → 发送给 LLM  │
└─────────────────────────────┘
```

---

#### NexaCode 上下文管理落地计划

结合 Rust + Tauri 技术栈，推荐的实现顺序：

| 阶段 | 功能 | 技术方案 | 优先级 |
|------|------|----------|--------|
| Phase 3 | Token 计数 | `tiktoken-rs` crate | 🔴 |
| Phase 3 | 工具输出截断 | ToolResult 加 `truncated` + `total_bytes` 字段 | 🔴 |
| Phase 3 | MicroCompact | 清理 >3 轮前的 tool_result，替换为简短摘要 | 🔴 |
| Phase 3 | AutoCompact | 调用 LLM 生成对话摘要（用同一个 provider） | 🔴 |
| Phase 3 | 上下文优先级 | ContextManager struct 管理优先级队列 | 🔴 |
| Phase 3 | 上下文裁剪 | 按优先级排序 + Token 预算裁剪 | 🔴 |
| Phase 3 | 对话历史滑动窗口 | 最近 3-5 轮完整保留 + 中间压缩 + 过早丢弃 | 🔴 |
| Phase 3 | Token 阈值体系 | 多级阈值：AutoCompact 触发 → 警告 → 错误 → 阻塞 | 🔴 |
| Phase 4 | 项目代码索引 | 文件树 + grep 搜索（已有） | 🟡 |
| Phase 4 | Full Compact / 手动 Compact | `/compact` 命令，支持自定义指令 | 🟡 |
| Phase 4 | 智能内容精简 | 代码文件精简、目录树精简、系统提示词精简 | 🟡 |
| Phase 5 | Embedding 语义检索 | 本地 Embedding (ort) + SQLite-VSS | 🟢 |
| Phase 5 | AST 感知分块 | Tree-sitter 解析，按函数/类边界切分 | 🟢 |
| Phase 5 | LSP 集成 | tower-lsp 或调用 VSCode LSP | 🟢 |

---

### 4. ✏️ Code Edit System（代码编辑系统）— Code Agent 的核心体验

#### 设计目标

- LLM 生成的代码修改以 **Diff** 形式呈现
- 用户可以 **逐个确认/拒绝** 每个修改
- 修改可以 **自动应用** 到文件系统
- 支持 **行级精确编辑**（不是全文替换）

#### Edit 工具设计要点

- 核心模式：SEARCH/REPLACE（类似 Aider）— 精确匹配原始文本，替换为新文本
- 多种编辑操作：精确替换、行后插入、删除行范围、创建文件、删除文件
- 描述字段：LLM 说明每次编辑的意图

#### Diff 预览 & 确认机制

- 前端以 unified diff 格式展示修改
- 每个修改可独立 Accept / Reject
- 支持 Accept All 批量接受

#### Tauri 命令

- `preview_edit` — 预览编辑的 Diff（不实际修改）
- `apply_edit` — 应用编辑
- `undo_edit` — 撤销编辑
- `get_pending_edits` — 获取待确认的编辑列表

---

### 5. 🖥️ Terminal Integration（终端集成）

#### 设计要点

- **沙箱执行**：限制可执行的命令范围（白名单 + 黑名单 + 危险模式正则匹配）
- **超时控制**：防止命令卡死
- **输出捕获**：实时捕获 stdout/stderr，过长时截断
- **用户确认**：危险命令需要用户确认
- **环境隔离**：在项目工作目录下执行，可配置环境变量

#### 用户确认分级

| 级别 | 命令示例 | 行为 |
|------|----------|------|
| 🟢 自动执行 | ls, cat, head, grep, git status, git diff, git log | 无需确认 |
| 🟡 需确认 | npm install, cargo build, git add, git commit | 弹窗确认 |
| 🔴 需确认+警告 | rm, git push, git reset, sudo, curl/wget | 确认 + 危险提示 |

---

### 6. 📂 Workspace / Project Awareness（项目感知）

#### 需要感知的信息

- 项目根目录
- 项目类型（Rust / Node.js / Python / Go / Java / 混合 / 未知）
- Git 仓库信息（分支、状态）
- 依赖列表
- 目录结构
- 配置文件列表
- 当前打开的文件
- Linter/编译器错误

#### 系统提示词动态生成

根据 WorkspaceInfo 动态生成系统提示词，包含：
- 当前项目信息（类型、根目录、Git 分支、打开文件）
- 精简目录树
- 可用工具列表及使用说明
- 编码规范（Read before Edit、SEARCH/REPLACE、Run tests、Commit 规范等）

---

### 7. 🔒 Safety & Permission System（安全与权限系统）

#### 权限分级

| 权限 | 默认行为 | 说明 |
|------|----------|------|
| FileRead | 默认允许 | 读取文件 |
| FileWrite | 需确认 | 写入/创建文件 |
| FileDelete | 需确认+警告 | 删除文件 |
| CommandExecute | 按风险等级 | 执行命令 |
| NetworkAccess | 默认禁止 | 网络访问 |

#### 用户确认机制

- Allow Once — 本次允许
- Allow Always — 始终允许（可配置作用域）
- Deny — 拒绝
- Remember this decision — 记住决策

#### 渐进式信任

- 读取操作：默认允许
- 搜索操作：默认允许
- 写入操作：需要确认
- 执行操作：根据风险等级确认
- 破坏性操作：确认 + 警告 + 可配置黑名单

---

### 8. 🔍 RAG & Code Intelligence（代码智能检索）

#### 语义搜索

- 本地 Embedding 索引：文件分块 → 向量化 → 相似度检索
- 支持增量更新（文件修改后更新索引）
- 远期：接入远程 Embedding API 平衡精度和成本

#### 符号索引（LSP 集成，远期目标）

- goto_definition — 跳转到定义
- find_references — 查找所有引用
- get_hover_info — 获取悬停信息
- get_diagnostics — 获取诊断错误

---

### 9. 📊 Conversation Memory（对话记忆）

#### 短期记忆（当前会话）

- 对话历史（带压缩）
- 当前 Agent 执行状态
- 最近读取/编辑的文件

#### 长期记忆（跨会话）

- 保存会话摘要
- 获取与当前任务相关的历史上下文
- 保存用户偏好（代码风格、常用命令等）

---

### 10. 🎨 Frontend Enhancements（前端增强）

#### Agent 执行视图

当前聊天界面需要支持展示 Agent 的"思考-行动"过程：
- 💭 思考步骤展示
- 📖 文件读取进度
- 🔍 搜索结果摘要
- ✏️ Diff 预览 + 确认/拒绝按钮
- ▶️ 终端命令执行 + 输出
- ✅ 验证结果（build/test 通过）

#### 新增前端组件

| 组件 | 说明 |
|------|------|
| AgentStep | Agent 单步执行展示（思考/工具调用/结果） |
| ToolCallView | 工具调用详情（参数 + 结果） |
| DiffPreview | 代码 Diff 预览（带确认按钮） |
| TerminalOutput | 终端输出展示 |
| FileTree | 项目文件树 |
| WorkspaceSelector | 工作区选择 |
| PermissionDialog | 权限确认弹窗 |
| AgentStatus | Agent 状态指示器（思考中/执行中/等待确认） |
| ContextViewer | 上下文查看器（当前 LLM 看到了什么） |

---

## 三、架构演进路线

### Phase 1：基础 Agent 框架（MVP）

**目标**：让 Agent 能读取文件、编辑代码、执行简单命令

- [ ] Tool System 基础框架（trait + registry + 5 个基础工具）
  - Read, Write, LS, Grep, Bash
- [ ] Agent Loop（单步工具调用）
- [ ] 工具调用 API 支持（OpenAI function calling / Anthropic tool_use）
- [ ] 前端 ToolCallView 展示
- [ ] 项目工作区选择

### Phase 2：代码编辑体验

**目标**：Diff 预览 + 确认机制

- [ ] Edit 工具（精确替换）
- [ ] Diff 预览组件
- [ ] 编辑确认/拒绝机制
- [ ] 撤销支持（自动 git stash）
- [ ] Terminal 输出展示

### Phase 3：智能上下文

**目标**：Agent 理解项目结构，智能管理上下文窗口

- [ ] Project Index（文件树索引）
- [ ] Token 计数（tiktoken-rs）
- [ ] Context Manager（Token 预算管理 + 优先级队列）
- [ ] 工具输出截断（ToolResult truncated + total_bytes）
- [ ] MicroCompact（清理 >3 轮前的 tool_result）
- [ ] AutoCompact（LLM 生成对话摘要，Token 阈值 ~87% 触发）
- [ ] 对话历史滑动窗口（最近 3-5 轮完整 + 中间压缩 + 过早丢弃）
- [ ] Token 阈值体系（AutoCompact → 警告 → 错误 → 阻塞）
- [ ] 上下文优先级排序与裁剪
- [ ] 动态系统提示词生成
- [ ] 打开文件自动纳入上下文

### Phase 4：安全 & 完善

**目标**：生产可用

- [ ] Permission System（权限确认）
- [ ] Terminal 沙箱
- [ ] Agent 中断机制
- [ ] 错误恢复（Agent 能自我修复）
- [ ] Git 集成（diff, commit, branch）
- [ ] Full Compact / 手动 Compact（`/compact` 命令，支持自定义指令）
- [ ] 项目代码索引（文件树 + grep 搜索）
- [ ] 智能内容精简（代码文件精简、目录树精简、系统提示词精简）

### Phase 5：高级功能

**目标**：差异化竞争力

- [ ] Embedding 语义检索（本地 Embedding (ort) + SQLite-VSS）
- [ ] AST 感知分块（Tree-sitter 解析，按函数/类边界切分）
- [ ] LSP 集成（符号跳转、引用查找、类型信息）
- [ ] 多 Agent 协作（Plan Agent + Code Agent + Review Agent）
- [ ] 长期记忆
- [ ] 自定义 Agent 配置（Agent Profiles）
- [ ] MCP (Model Context Protocol) 支持
- [ ] 多文件编辑编排
- [ ] 代码审查模式

---

## 四、LLM Provider 扩展需求

当前 `LLMProvider` trait 只支持 `chat` 和 `chat_stream`，需要增加：

### 新增方法

- `chat_with_tools()` — 带工具定义的聊天，返回文本或工具调用
- `chat_stream_with_tools()` — 带工具定义的流式聊天

### 新增类型

- `ToolDefinition` — 工具定义（name + description + parameters JSON Schema）
- `ToolAwareResponse` — 工具感知响应（Text 或 ToolCalls）
- `ToolCall` — 工具调用（id + name + arguments）

### Message 类型扩展

当前只有 System / User / Assistant，需要新增：
- `AssistantToolCalls` — LLM 请求调用工具
- `ToolResult` — 工具执行结果（tool_call_id + content + is_error）

---

## 五、关键设计原则

### 1. Human-in-the-Loop（人机协同）

Agent 的每一步关键操作（文件修改、命令执行）都应该有用户确认机制。信任度可以逐步提升：
- 新用户：每步确认
- 熟练用户：只确认危险操作
- 专家用户：自动执行 + 事后审查

### 2. 可观测性（Observability）

用户必须能看到 Agent 在做什么：
- 当前在思考什么
- 调用了什么工具
- 传入了什么参数
- 得到了什么结果
- 即将做什么修改

### 3. 可逆性（Reversibility）

Agent 的所有文件修改都应该可撤销：
- 自动 git stash 修改前的状态
- 支持 undo 操作
- 修改历史可追溯

### 4. 上下文效率（Context Efficiency）

Token 是最宝贵的资源：
- 每次只发送与当前任务相关的上下文
- 避免重复读取同一文件
- 智能裁剪过长的文件内容
- 历史对话及时压缩摘要

### 5. 渐进式信任（Progressive Trust）

- 读取操作：默认允许
- 搜索操作：默认允许
- 写入操作：需要确认
- 执行操作：根据风险等级确认
- 破坏性操作：确认 + 警告 + 可配置黑名单

---

## 六、参考项目

| 项目 | 特点 | 值得借鉴 |
|------|------|----------|
| **Cursor** | IDE 深度集成、Tab 补全、Composer | Diff 预览、多文件编辑、LSP 集成 |
| **Claude Code** | 终端 Agent、自主执行 | Agent Loop 设计、工具系统、权限模型 |
| **Windsurf** | Cascade 流、上下文感知 | 上下文管理、Flow 设计 |
| **Aider** | Git 集成、多模型支持 | SEARCH/REPLACE 编辑格式、Git 工作流 |
| **Cline** | VSCode 插件、分步确认 | 确认机制、工具调用可视化 |
| **OpenHands** | 沙箱执行、多 Agent | 沙箱设计、Agent 编排 |

---

## 七、技术选型建议

| 需求 | 推荐方案 | 理由 |
|------|----------|------|
| 代码搜索 | ripgrep (grepxxx crate) | 性能极好，Rust 原生 |
| 语义搜索 | 本地 Embedding (ort + small model) 或远程 API | 平衡精度和成本 |
| 终端执行 | tokio::process::Command + pty | 异步、输出捕获 |
| Git 操作 | git2 crate | 纯 Rust，无需 git CLI |
| Diff 生成 | similar crate | Rust 原生 diff 库 |
| JSON Schema | schemars crate | 自动从 Rust 类型生成 JSON Schema |
| Token 计数 | tiktoken-rs | OpenAI tokenizer，准确计算 Token |
| 文件监控 | notify crate | 文件变更检测，增量更新索引 |
| 向量数据库 | SQLite-VSS | 轻量本地存储，无需外部服务 |
| AST 解析 | tree-sitter crate | 增量解析，支持多语言，语义感知分块 |
| LSP 集成 | tower-lsp | Rust 原生 LSP 框架 |
