# NexaCode 项目概览

## 项目简介

NexaCode 是一款**基于 LLM 的 AI 聊天桌面应用**，类似于 ChatGPT Desktop 或 Claude Desktop，但支持多种 AI 提供商的灵活切换。

## 技术架构

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | React 19 + TypeScript + Vite | 聊天界面、会话管理、Markdown 渲染 |
| 桌面框架 | Tauri 2.0 | 轻量级跨平台桌面应用，前后端通过 IPC 通信 |
| 后端 | Rust | LLM 客户端、提供商管理、会话持久化 |

## 项目结构

```
NexaCode/
├── crates/
│   ├── nexacode-core/           # Rust 核心库
│   │   ├── src/
│   │   │   ├── lib.rs           # 库入口，导出 llm 和 session 模块
│   │   │   ├── llm/             # LLM 客户端抽象层
│   │   │   │   ├── client.rs    # LLMClient 实现
│   │   │   │   ├── traits.rs    # LLMProvider trait 定义
│   │   │   │   ├── types.rs     # 共享类型定义
│   │   │   │   └── providers/   # 具体提供商实现
│   │   │   │       ├── openai.rs        # OpenAI 提供商
│   │   │   │       ├── anthropic.rs     # Anthropic (Claude) 提供商
│   │   │   │       └── base.rs          # 基础提供商（OpenAI-Compatible）
│   │   │   └── session/         # 会话持久化存储
│   │   ├── examples/
│   │   │   └── test_llm.rs     # LLM 测试示例
│   │   └── tests/
│   │       └── llm_test.rs     # LLM 集成测试
│   └── nexacode-desktop/        # Tauri 桌面应用
│       ├── src/
│       │   ├── main.rs          # 应用入口
│       │   ├── lib.rs           # Tauri 命令注册 & 应用配置
│       │   └── llm/             # LLM 相关 Tauri 命令
│       │       ├── commands.rs  # Tauri 命令实现
│       │       └── manager.rs   # LLMManager 状态管理
│       ├── capabilities/
│       │   └── default.json     # Tauri 权限配置
│       ├── tauri.conf.json      # Tauri 应用配置
│       └── icons/               # 应用图标
├── src/                         # React 前端
│   ├── App.tsx                  # 主应用组件（侧边栏 + 聊天区）
│   ├── main.tsx                 # 前端入口
│   ├── components/
│   │   ├── Settings.tsx         # AI 提供商配置界面
│   │   ├── MarkdownRenderer.tsx # Markdown 渲染（代码高亮）
│   │   ├── ChatExample.tsx      # 聊天示例组件
│   │   └── LucideIcon.tsx       # Lucide 图标组件
│   ├── hooks/
│   │   └── useLLM.ts            # React Hook：管理聊天状态和流式内容
│   ├── services/
│   │   └── llm.ts               # 封装 Tauri IPC 调用的 LLM 服务
│   ├── styles/
│   │   ├── main.scss            # 样式入口
│   │   ├── _chat.scss           # 聊天区样式
│   │   ├── _content.scss        # 内容区样式
│   │   ├── _sidebar.scss        # 侧边栏样式
│   │   ├── _variables.scss      # SCSS 变量
│   │   ├── _mixins.scss         # SCSS Mixins
│   │   └── _reset.scss          # CSS Reset
│   └── assets/
│       └── logo.png             # 应用 Logo
├── dist/                        # 前端构建产物
├── package.json                 # Node.js 依赖
├── Cargo.toml                   # Rust workspace 配置
├── vite.config.ts               # Vite 构建配置
└── config.example.toml          # 配置文件示例
```

## 核心模块说明

### 1. nexacode-core（Rust 核心库）

- **llm/** — LLM 客户端抽象层
  - `LLMProvider` trait 定义了 `chat()`、`chat_stream()`、`list_models()` 等接口
  - 三个具体实现：OpenAI、Anthropic、OpenAI-Compatible（支持 Ollama、vLLM、LocalAI 等任何 OpenAI 兼容 API）
- **session/** — 聊天会话持久化存储
  - `Session`、`SessionMeta`、`ChatMessage` 等数据结构
  - 支持会话的创建、加载、保存、删除

### 2. nexacode-desktop（Tauri 桌面应用）

- 通过 Tauri Commands 暴露 Rust 后端能力给前端
- `LLMManager` 管理多个 LLM 提供商的状态，支持切换活跃提供商
- 启动时自动从磁盘加载配置（`~/.nexacode/config.toml`）

### 3. 前端（React）

- **App.tsx** — 主界面布局：左侧可拖拽调整宽度的侧边栏（会话列表）+ 右侧聊天区域
- **useLLM Hook** — 管理聊天状态，处理流式响应，自动更新 UI
- **LLMService** — 封装所有 Tauri IPC 调用（`invoke` + `listen` 事件）

## Tauri 命令清单

### 提供商管理

| 命令 | 说明 |
|------|------|
| `add_provider` | 添加 LLM 提供商 |
| `remove_provider` | 移除提供商 |
| `set_active_provider` | 设置当前活跃提供商 |
| `list_providers` | 列出所有已配置的提供商 |
| `get_active_provider` | 获取当前活跃提供商名称 |
| `get_provider_config` | 获取指定提供商的配置 |
| `update_provider` | 更新提供商配置 |
| `load_providers` | 从磁盘加载提供商配置 |
| `list_models` | 列出可用模型 |

### 聊天操作

| 命令 | 说明 |
|------|------|
| `chat` | 非流式聊天 |
| `chat_stream` | 流式聊天（通过 Tauri 事件推送：`chat-chunk`、`chat-error`、`chat-end`） |

### 会话管理

| 命令 | 说明 |
|------|------|
| `list_sessions` | 列出所有会话 |
| `load_session` | 加载指定会话的消息 |
| `save_session` | 保存会话到磁盘 |
| `delete_session` | 删除指定会话 |

## 支持的 LLM 提供商

1. **OpenAI** — GPT-4、GPT-4o、GPT-3.5 Turbo 等
2. **Anthropic** — Claude 3.5 Sonnet、Claude 3.5 Haiku、Claude 3 Opus 等
3. **OpenAI-Compatible** — 任何兼容 OpenAI API 的服务：
   - Ollama（本地模型）
   - vLLM
   - LocalAI
   - 其他自定义端点

## 配置文件

- **位置**: `~/.nexacode/config.toml`（macOS/Linux），`C:\Users\<username>\.nexacode\config.toml`（Windows）
- **格式**: TOML
- **自动加载**: 应用启动时自动读取，无需手动加载
- 参见 `config.example.toml` 获取格式示例

## 关键数据流

```
用户输入 → React App.tsx → useLLM Hook → LLMService (IPC invoke)
  → Tauri Command (Rust) → LLMManager → LLMProvider (OpenAI/Anthropic/...)
  → HTTP 请求 → AI 服务端
  ← 流式事件回传: chat-chunk / chat-error / chat-end
  ← LLMService (IPC listen) → useLLM Hook → React UI 更新
```

## 开发命令

```bash
npm install          # 安装前端依赖
npm run tauri dev    # 启动开发模式（前端 + Tauri 窗口）
npm run tauri build  # 构建生产版本
```

## 扩展新 LLM 提供商

1. 在 `crates/nexacode-core/src/llm/providers/` 中实现 `LLMProvider` trait
2. 在 `ProviderType` 枚举中添加新的提供商类型
3. 在 `LLMClient::new()` 中处理新类型
4. 前端无需修改

## 项目状态

- 版本：0.1.0（早期开发阶段）
- 许可证：MIT
- 仓库：https://github.com/fansili/NexaCode
